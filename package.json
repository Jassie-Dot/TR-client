{
  "name": "tr-enterprises-premium-fullstack",
  "version": "2.0.0",
  "private": true,
  "description": "Premium full-stack Node.js and Tailwind website for TR Enterprises.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css --minify",
    "build:scene": "esbuild ./public/scene.js --bundle --format=iife --global-name=TRScrollScene --outfile=./public/scene.bundle.js --minify",
    "build": "npm run build:css && npm run build:scene",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css --watch",
    "check": "node --check server.js && node --check public/app.js && node --check public/scene.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
